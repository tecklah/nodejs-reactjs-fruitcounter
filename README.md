# nodejs-reactjs-fruitcounter

A sample project using ReactJS to create a simple form for user to key in a fruit name (any word) and send it to backend server (ExpressJS) for counting the number of occurrence. A pie chart is created using Highchart to show the count for each type of fruits.
